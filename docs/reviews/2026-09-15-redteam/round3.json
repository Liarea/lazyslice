{
 "round": 3,
 "date": "2026-09-16",
 "tree": "ee2c64f",
 "attackers": 4,
 "attempts": 64,
 "leaked": 17,
 "attempts_list": [
  {
   "attack": "R2-01 / A2 replay — values crafted to miss every regex/parser, in columns with no name rule: a full name in `label`, an obfuscated email in `handle2`, a phone spelled in words in `contact`, a UK NI number with spelled digits in `ident`, a `.`-separated payment card in `pan`, a wordy address in `whereabouts`. 300 rows, table `records`.",
   "reproduction": "# lab (rebuilt from scratch; the round-2 scratch dir was gone)\ndocker run -d --name rt0-src -e POSTGRES_PASSWORD=pw -e POSTGRES_USER=ls -e POSTGRES_DB=postgres -p 55431:5432 postgres:16\ndocker run -d --name rt0-tgt -e POSTGRES_PASSWORD=pw -e POSTGRES_USER=ls -e POSTGRES_DB=postgres -p 55432:5432 postgres:16\ncd <repo> && go build -o /private/tmp/claude-501/lazyslice-scratch/redteam-0/lazyslice ./cmd/lazyslice\n# harness (recreated): /private/tmp/claude-501/lazyslice-scratch/redteam-0/run.sh creates src_<name>/tgt_<name>,\n# loads the schema, and runs:\n#   LAZYSLICE_SECRET=<64 hex> ./lazyslice --source 'postgres://ls:pw@127.0.0.1:55431/src_<name>?sslmode=disable' \\\n#     --target 'postgres://ls:pw@127.0.0.1:55432/tgt_<name>?sslmode=disable' --yes --no-config\n# check.sh pg_dumps the target --data-only and greps for each needle.\ncd /private/tmp/claude-501/lazyslice-scratch/redteam-0\n./run.sh a2 a2_values.sql\n./check.sh a2 'AB nine eight seven six five four D' 'QQ one two three four five six C' 'Grace Hopper' 'grace.hopper' '4539.1488' 'Rowan House' 'oh seven seven oh oh' 'nine double oh one two three'\ndocker exec rt0-tgt psql -U ls -d tgt_a2 -x -c 'select contact,ident from records order by id limit 2'",
   "leaked": true,
   "fix": "PARTLY FIXED, STILL LEAKS. Exit 0. Five of the six columns are now caught: `ident` (the spelled NI number) reports '100/200 samples parse as national identifiers' and is masked — the T-0187 national_id wiring closed the A2/A6/A7 root cause; `label`, `handle2`, `pan`, `whereabouts` are all masked. `contact` is NOT: it reports 'no name or value signal' and all 300 rows cross verbatim (150 hits each for 'oh seven seven oh oh' and 'nine double oh one two three'). Round 2 recorded `contact` as caught as free_text; on this tree it is caught by nothing, so this is a regression inside a fixed attempt, not a leftover.\n\nRoot cause, verified in internal/textsig (temporary probe test, removed afterwards — no repo file changed): the only thing that could have caught `contact` are (a) textsig.ValidPhone and (b) Dict.Prose. (a) fails because internal/textsig/textsig.go:49 sets PhoneRegionHint = \"ZZ\", so libphonenumber will only parse a number that is already in +E.164 form. candidates.go's spelledDigits does the right thing — spelledDigits(\"plus four four seven nine one one one two three four five six\") returns \"+447911123456\" and ValidPhone says true — but a NATIONAL-format spelling yields \"07700900123\" with no '+', and ValidPhone returns false for it and for every other national spelling (probed: '07911123456', '+44 7700 900123', '020 7946 0958' — all false). (b) fails because internal/textsig/dict.go:195 Prose = at-least-6-fields AND ContainsName, and a string of digit-words contains no dictionary name.\n\nSecondary, and worth fixing on its own: textsig.go:82-84 documents ValidPhone as reading 'every spelling Candidates yields, so a number dictated in words — \"plus four four, seven seven oh oh, nine one one, nine one one\" — is the number it spells'. That claim is true only for the international spelling; the national one, which is how people actually write a domestic number, is not covered, and the comment reads as though the class is closed.\n\nFix: give the phone validator a national-format path rather than relying on '+' surviving the candidate. Either (1) parse each candidate a second time under a configured default region (a `--phone-region`/yml setting, defaulting to the source cluster's lc_* or to a documented value) so '07911 123456' and its spelled form parse, or (2) have candidates.go emit an additional '+<cc>' candidate for a national-format digit run under that same region hint. (1) is the smaller change and keeps candidates.go honest about only respelling, not guessing a country. Whichever is chosen, add a regression under testdata/regressions/ with a national-format number both plain and dictated, and correct the ValidPhone doc comment so it states the region limit instead of implying the dictated case is closed. Note this is a recall change against ADR-010/T1, so it needs the T1 review internal/classify/CLAUDE.md demands.",
   "attacker": "attack:1:r3"
  },
  {
   "attack": "R2-02 / A6 replay — PLAIN, unobfuscated national identifiers in columns whose names miss the rule pack, in a table with no other personal column: `ref_code` holds hyphenated US SSNs (078-05-NNNN), `ni_ref` holds UK NI numbers (ABNNNNNND), 300 rows, table `benefit_claims`.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-0\n./run.sh a6 a6_natid.sql\n./check.sh a6 '078-05-' 'AB100' 'AB1000'\ndocker exec rt0-tgt psql -U ls -d tgt_a6 -c 'select id,ref_code,ni_ref from benefit_claims order by id limit 3'",
   "leaked": false,
   "fix": "FIXED. Exit 0 and nothing leaked. Both columns now report '200/200 samples parse as national identifiers; no name signal' and are masked: the target reads `1 | 545250819 | 710375382` against a source of `078-05-1001 | AB100001D`, and all three needles come back at 0 hits in the target dump. The T-0187 change (internal/classify/classify.go:371-372, the structured/checksum-only split, and internal/verify/validators.go:263-264) is what closes it. No further fix needed for this attempt; the reason string is now an affirmative finding rather than the misleading 'no name or value signal' round 2 complained about.",
   "attacker": "attack:1:r3"
  },
  {
   "attack": "R2-03 / A7 replay — the same national_id gap through three other carriers: a `text[]` of UK NI numbers (`id_list`), a plain SSN in a column called `code`, and a JSON document in a `text` column whose only personal datum is an SSN at depth 3 (`doc`). 300 rows, table `audit_docs`.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-0\n./run.sh a7 a7_natid_nested.sql\n./check.sh a7 '078-05-' 'AB100' 'JG200' '123-45-'\ndocker exec rt0-tgt psql -U ls -d tgt_a7 -x -c 'select doc,id_list,code from audit_docs order by id limit 1'",
   "leaked": false,
   "fix": "FIXED for the two carriers that leaked. Exit 0, 0 hits on all four needles. `id_list` reports '400/400 samples parse as national identifiers ... classified on the element type text' and comes back as `{710375382,471704898}`; `code` reports the same and is masked. So the array path and the scalar path both reach the new validator, which is what the round-2 fix note asked for.\n\nOne residual worth recording, unchanged from round 2 and not a leak: `doc` is still masked only because textsig.LooksSecret's entropy guess fires ('200/200 samples look like secrets'), replacing the whole JSON document with the fixed literal `$lazyslice$invalid`. The SSN at depth 3 is therefore not leaked, but it is not the JSON path that saved it, and the developer gets an unparseable document where a structured JSON masker would have preserved shape. Fix: keep the classifier's JSON-leaf path ahead of the credential entropy guess for a value that parses as JSON, so `doc` is masked leaf-by-leaf (SSN masked, `event`/`role`/`ts` preserved) rather than flattened — file as a tracker item under the T1 residual rather than treating it as a regression, since nothing leaks today.",
   "attacker": "attack:1:r3"
  },
  {
   "attack": "R2-04 / A9b replay — an SSN stored as `bigint` (`taxref`) and a phone stored as `numeric` (`msisdn`), no Luhn-valid card in the table so the second net's other numeric entry cannot fire. 300 rows, table `payroll`.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-0\n./run.sh a9b a9b_numeric.sql\n./check.sh a9b '78051' '44770090'\ndocker exec rt0-tgt psql -U ls -d tgt_a9b -c 'select id,taxref,msisdn from payroll order by id limit 4'\n# diagnostic run that isolates the cause (same attack, values spread instead of sequential,\n# no personal neighbour at all):  ./run.sh diag diag_nodense_nocorrob.sql\n#   payroll3(taxref bigint = 100000000 + floor(random()*799999999), amount numeric), 300 rows",
   "leaked": true,
   "fix": "STILL LEAKS. Exit 0. `msisdn` is saved by its name matching phone and is masked to a fake number, but `taxref` reports 'no name or value signal' and all 300 SSNs cross verbatim (300 hits for '78051'; target reads `1 | 78051001`). The diagnostic run leaks identically (exit 0, 300 sequential-free random 9-digit SSNs in the target).\n\nRoot cause on this tree is no longer 'no validator' — internal/verify/validators.go:344-349 now has the digits entry (textsig.ValidNationalIDDigits, minRatio=nationalIDDigitsThreshold 0.97, sequenceExempt, requiresCorroboration). Two of its three gates are what let the value through, and the two runs separate them:\n  1. sequenceExempt / digitRange. The replay's values are 78051001..78051300, a dense block, so the column is exempted before its ratio is scored. That exemption is defensible for a surrogate key, but a block of sequentially ISSUED identifiers (a payroll import, a benefits batch, a migrated legacy range) is exactly as dense and exactly as personal, and it is exempt by construction.\n  2. requiresCorroboration. The diagnostic run's values are sparse, so density does not save them — the column leaks anyway because nothing in `payroll3` corroborates: rules.yml's national_id name pattern does not match `taxref`, and `amount` is not a proven personal column. Comparing this with the A9b variant below (same sparse values PLUS a proven email neighbour → exit 9, refused), corroboration is the load-bearing gate, and it makes the control conditional on the attacker being careless enough to leave a recognisable personal column in the same table.\n\nFix, in two parts and both are ADR-level recall trades, not patches:\n  a. Narrow sequenceExempt so it exempts a dense run only when the column is also the table's key or is provably generated (a `serial`/identity default, or an `int`/`bigint` column with a sequence attached) — internal/verify already reads enough catalog to know. A dense non-key bigint column with no sequence behind it is a loaded block, not a counter, and should be scored rather than skipped.\n  b. requiresCorroboration should accept a third corroborator that does not depend on a neighbour: the column's own value distribution passing the SSA exclusion ranges at ratio 1.0 while being neither a date nor a generated sequence, over a sample large enough to matter. Failing that, the honest alternative is to stop calling this a control for the uncorroborated case and say so in THREAT_MODEL.md T1 — the tree today reports 'no name or value signal' for a column of real SSNs, which reads as a finding of absence.\nFile under T-0195's neighbourhood; it needs the T1 review and a TestPagilaPrecisionAndRecall measurement, not a quiet threshold edit.",
   "attacker": "attack:1:r3"
  },
  {
   "attack": "R2-05 / A10 replay — a person's full name the dictionary does not carry, in a column called `label` whose name matches no rule, in a table with no other personal column: Khmer, Lao and Amharic transliterations (Sokhem Chanthavy, Bounmy Phimmasone, Tesfaye Gebreselassie, Rithy Sopheak, Khamla Vongsa). 300 rows, table `entries`.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-0\n./run.sh a10 a10_dictmiss.sql\n./check.sh a10 'Sokhem' 'Bounmy' 'Tesfaye' 'Rithy' 'Khamla'",
   "leaked": true,
   "fix": "STILL LEAKS, and nothing about it has changed since round 2. Exit 0, 60 hits for each of the five names, column reported 'no name or value signal'.\n\nThis remains the acknowledged THREAT_MODEL.md T1 residual (line 41: 'a name in a language the dictionary does not carry, in a column named in that language, with no other signal in the table'), so I report it as reproducing rather than as new. What has NOT happened is either of the two cheap things round 2 asked for:\n  1. The reason string is still the bare 'no name or value signal'. For a character column that was sampled and matched nothing, that is a finding of absence printed where only absence of evidence exists, and it is the same sentence the tool prints for an integer column it never looked inside. Change it for the sampled-character case to something that says what actually happened — 'nothing recognised in 200 samples; not proof the column is impersonal'. This is a render/reasons change (internal/classify/reasons.go), costs no recall, and is the honest half.\n  2. unknownColumnsBesideCertain still fires only inside a table that already holds a `certain` person-identifying column, which is precisely the table this attack does not have. A table whose only unexplained column is a short, low-cardinality, non-unique character column with no type conflict is worth a `possible`, or at minimum a printed prompt in interactive mode. That is a recall/precision trade — it belongs in the tracker as an ADR question under the T1 residual, not in this fix round.",
   "attacker": "attack:1:r3"
  },
  {
   "attack": "NEW VARIANT of R2-01/A2 — the surviving `contact` hole taken off the reserved drama range and made ordinary: REAL, valid national-format UK phone numbers, both dictated in words (`contact`) and written plainly with spaces and brackets (`kontaktnr`, an abbreviation in another language, which is the column-name half of the surface). No obfuscation of any kind in `kontaktnr`. 300 rows, table `tickets`.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-0\ncat a2v_phone.sql   # tickets(id, contact text, kontaktnr text, note text)\n                    # contact:   'oh seven nine one one one two three four five six' | 'oh two oh seven nine four six oh nine five eight'\n                    # kontaktnr: '07911 123456' | '020 7946 0958' | '(0161) 496 0123'\n./run.sh a2v a2v_phone.sql\n./check.sh a2v 'oh seven nine one one' 'oh two oh seven nine four six' '07911 123456' '020 7946 0958' '0161'\ndocker exec rt0-tgt psql -U ls -d tgt_a2v -c 'select id,contact,kontaktnr from tickets order by id limit 3'",
   "leaked": true,
   "fix": "LEAKED, and this is the severe form of the A2 residual. Exit 0. Both columns report 'no name or value signal'. 150 hits each for the two dictated numbers, 100 hits each for '07911 123456', '020 7946 0958' and '0161' — the target rows read `1 | oh two oh seven nine four six oh nine five eight | 020 7946 0958`. A plain, correctly formatted domestic phone number in a column called `kontaktnr` is copied to the target verbatim under a green tick, with no obfuscation and no exotic encoding. (The only column masked in the table was `note`, on its name.)\n\nSame root cause as the A2 residual — PhoneRegionHint = \"ZZ\" in internal/textsig/textsig.go:49 means libphonenumber parses nothing that is not already +E.164, so every national spelling scores zero, and Dict.Prose cannot see a string of digits or digit-words. The variant matters because it removes the defence that the A2 case was contrived: the attacker did not have to spell anything out, and `kontaktnr`/`contact` are names a real ticketing or CRM schema would use.\n\nFix as in the A2 entry: parse candidates a second time under a configured default region (flag + yml, surfaced in the reasons output so the operator can see which region was assumed), so both '07911 123456' and its dictated form are a phone; keep the strong/any-hit-fails footing the text phone entry already has in internal/verify/validators.go. Pin both spellings in testdata/regressions/, and pin a national-format number under a DIFFERENT region as a non-hit so the region assumption is visible rather than silent. ValidPhone's doc comment ('a national-format column reaches the classifier through its name, at possible, and is masked anyway') is the assumption this variant falsifies — a name that is not in rules.yml gets nothing — so that comment and THREAT_MODEL.md T1 need correcting alongside the code.",
   "attacker": "attack:1:r3"
  },
  {
   "attack": "NEW VARIANT of R2-04/A9b — the same bigint-SSN carrier, rebuilt into the shape the current control claims to catch: values spread across the whole SSA range rather than a dense sequence (so digitRange's sequence exemption cannot apply), and a PROVEN personal neighbour in the same table (real email addresses, name and value both certain), which is the corroboration internal/verify/validators.go names as the escape. 300 rows, table `payroll2`.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-0\ncat a9bv_corrob.sql  # payroll2(id serial pk, taxref bigint = 100000000 + floor(random()*799999999), email text = 'personN@bigcorp.example')\n./run.sh a9bv a9bv_corrob.sql\ndocker exec rt0-tgt psql -U ls -d tgt_a9bv -c 'select id,taxref,email from payroll2 order by id limit 3'   # relation does not exist",
   "leaked": false,
   "fix": "DID NOT LEAK, and it failed loudly and correctly. Exit 9. The run reports '✗ public.payroll2.taxref is not masked and 298 of its values validate as national_id' and 'lazyslice: verify.refused.second_net: verify: verify.refused.second_net on public.payroll2.taxref: national_id', then drops the target table AND the orphaned sequence ('dropping object public.payroll2_id_seq in the target: it was created by a run that left personal data behind'). Reading the target back afterwards errors with 'relation payroll2 does not exist' — nothing personal is left in a half-loaded target, which is A2-of-the-threat-model (never half-loaded and green) behaving as specified.\n\nNo fix needed for this attempt; its value is diagnostic. Taken with the A9b replay and the sparse/uncorroborated diagnostic run in that entry, it shows the digits national_id entry is live and effective, and that the two ways past it are exactly sequenceExempt (dense issued blocks) and requiresCorroboration (no recognisable personal column in the table) — which is where the A9b fix should be aimed, rather than at the validator or its ratio.",
   "attacker": "attack:1:r3"
  },
  {
   "attack": "NEW VARIANT of R2-05/A10 — the same dictionary-miss class taken one step further: the names written in their NATIVE SCRIPT (Amharic, Khmer, Lao, Thai) in a column whose NAME is also in that script (`ስም`, Amharic for 'name'), in a table with no other personal column. 300 rows, table `registrations`.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-0\ncat a10v_script.sql  # registrations(id serial pk, \"ስም\" text, qty int)\n                     # values: 'ተስፋዬ ገብረሥላሴ','ብርሃኑ አበበ','សុខេម ចន្ថាវី','ຄຳລາ ວົງສາ','ริทธี โสภาค'\n./run.sh a10v a10v_script.sql\n./check.sh a10v 'ተስፋዬ' 'ብርሃኑ' 'សុខេម' 'ຄຳລາ' 'ริทธี'\ndocker exec rt0-tgt psql -U ls -d tgt_a10v -c 'select id,\"ስም\" from registrations order by id limit 3'",
   "leaked": true,
   "fix": "LEAKED. Exit 0, 60 hits for each of the five names, and the column is reported as 'public.registrations.ስም: no name or value signal'. The tool handled the non-ASCII identifier and the UTF-8 values correctly throughout — it quoted and recreated the column, sampled it, and then said it found nothing — so this is a recall miss, not a mishandling.\n\nIt is the same T1 residual as A10, with one addition that is a concrete, bounded fix rather than an unbounded dictionary problem: rules.yml's name patterns are ASCII-only, so a column literally named 'name' in Amharic, Khmer, Lao, Thai, Greek, Cyrillic, Arabic or CJK matches nothing. Widening the NAME rule pack to carry the word for name/surname/address/phone/email in the twenty or so scripts a production schema plausibly uses is finite, testable, and costs no value-side recall — unlike widening names.txt, which the round-2 note rightly refused. That alone would have caught this table.\n\nTwo things to do, both filed rather than slipped in: (1) add non-ASCII name-rule entries to rules.yml with a regression fixture per script, and state in internal/classify/CLAUDE.md that the rule pack is expected to be multilingual so the next reviewer sees the gap; (2) the reason-string change from the A10 entry applies here with extra force — a column named in a script the rule pack cannot read should not report the same sentence as a column the rules examined and cleared.",
   "attacker": "attack:1:r3"
  },
  {
   "attack": "R2-06 replay (the one item on docs/reviews/2026-09-15-redteam/round2-still-leaking.json that belongs to the wrong-target surface): reach ONE production cluster over two transports so that the gate's cluster identity differs by spelling. Source = app over TCP as the SELECT-only role ARCHITECTURE.md §9 recommends (no EXECUTE on pg_control_system, so system_identifier is \"\" on the source side); target = the empty production database newprod on the SAME cluster, named over the Unix socket. In round 2 this produced no 'same cluster' line at all and the run dropped and reloaded tables in a production database in silence.",
   "reproduction": "B=/private/tmp/claude-501/lazyslice-scratch/redteam-1/r2; mkdir -p $B/sock\ngo build -o $B/lazyslice ./cmd/lazyslice   # rebuilt at 22ae0b2, clean tree\n/opt/homebrew/bin/initdb -D $B/pgdata -U postgres --auth=trust\n/opt/homebrew/bin/pg_ctl -D $B/pgdata -o \"-p 15600 -h 127.0.0.1 -k $B/sock\" -l $B/pg.log start\n# db app: customers(email,full_name,phone,ssn) 50 rows + orders 200 rows; db newprod: the same two tables, EMPTY\n# CREATE ROLE reader LOGIN PASSWORD 'pw'; GRANT CONNECT/USAGE/SELECT only\npsql postgres://postgres@127.0.0.1:15600/app -c \"REVOKE EXECUTE ON FUNCTION pg_control_system() FROM public;\"\n#   NOTE: function ACLs are per-database (pg_proc). The round-2 repro revoked in db 'postgres' only, which does NOT\n#   deny the role in db 'app'; verify the premise first:\npsql -tA postgres://reader:pw@127.0.0.1:15600/app -c 'select system_identifier from pg_control_system()'   # -> permission denied\nexport LAZYSLICE_SECRET=$(openssl rand -hex 32)\n# CONTROL, TCP target on the same cluster:\n$B/lazyslice --source 'postgres://reader:pw@127.0.0.1:15600/app' --target 'postgres://postgres@127.0.0.1:15600/newprod' --yes --no-config -n 5\n#  -> '! target newprod is on the same cluster as the source'   (grep -c 'same cluster' = 1)\n# ATTACK, same cluster, spelled as the socket:\n$B/lazyslice --source 'postgres://reader:pw@127.0.0.1:15600/app' --target \"postgres://postgres@/newprod?host=$B/sock&port=15600\" --yes --no-config -n 5\n#  -> '! target newprod is on the same cluster as the source: this run writes to the source's own server'\n#     grep -c 'same cluster' = 1, exit=0",
   "leaked": false,
   "fix": "No fix needed for this exact attempt: b5e6f3d (T-0190) closed it. sqlClusterID (internal/pg/source.go:352) now carries only cluster properties — to_char(pg_postmaster_start_time() AT TIME ZONE 'UTC'), the maintenance-db oid, data_directory from pg_settings, server_version — and sameClusterIdentity skips a field one side could not read instead of reading it as a difference, so the TCP session and the socket session produce identities that compare equal (measured: '2026-09-16 19:21:49.595485|14225||14.18' vs '...|14225|/private/.../pgdata|14.18'). Verified by hand and covered by internal/pg's cluster tests. One thing NOT done from the round-2 fix text, and it is what my variant below turns on: item (3), 'ARCHITECTURE.md §9's recommended-role snippet should append GRANT EXECUTE ON FUNCTION pg_control_system() TO lazyslice_ro'. The snippet the run header prints (internal/core/run.go's CodeRoleWritable message, seen verbatim in my --where run below) still grants only CONNECT/USAGE/SELECT, so every operator who follows it creates a source role whose strongest cluster identity is unreadable and whose weaker one is a single revoke away from being unreadable too.",
   "attacker": "attack:2:r3"
  },
  {
   "attack": "One new variant of R2-06, since the fix it landed rests entirely on pg_postmaster_start_time() being readable by the source role: deny the source role that function too (a hardened production cluster that revokes monitoring functions from PUBLIC — the same class of hardening the original finding's premise assumes for pg_control_system), then reach the same cluster over the socket again. With both identity functions denied, Source.ClusterID returns \"\" with a nil error (internal/pg/source.go:236-240 swallows any non-context error), the gate's clusterKnown is false and sourceSystemID is \"\", so Target.Gate falls through to the last arm — targetRef.SameCluster(source), a comparison of normalised host:port — and 'unix:/private/.../sock' does not equal 'localhost'. Rule 1's fail-closed sameCatalog arm cannot save it either, because it only fires when currentDB == source.Database and the target database is called newprod, not app.",
   "reproduction": "# same lab as above, still running\npsql postgres://postgres@127.0.0.1:15600/app -c \"REVOKE EXECUTE ON FUNCTION pg_postmaster_start_time() FROM public;\"\npsql -tA postgres://reader:pw@127.0.0.1:15600/app -c 'select pg_postmaster_start_time()'   # -> permission denied\npsql postgres://postgres@127.0.0.1:15600/postgres -c 'DROP DATABASE newprod' -c 'CREATE DATABASE newprod'\npsql postgres://postgres@127.0.0.1:15600/newprod -f $B/schema.sql        # empty production database\nexport LAZYSLICE_SECRET=$(openssl rand -hex 32)\n$B/lazyslice --source 'postgres://reader:pw@127.0.0.1:15600/app' \\\n  --target \"postgres://postgres@/newprod?host=$B/sock&port=15600\" --yes --no-config -n 5\n#  source app on 127.0.0.1 as reader (flag) - --source\n#  target newprod on /private/.../sock - --target\n#  (NO same-cluster line: grep -c 'same cluster' = 0)\n#  dropping public.orders in the target\n#  dropping public.customers in the target\n#  public.customers: 5 rows / public.orders: 20 rows\n#  exit=0\npsql -tA postgres://postgres@127.0.0.1:15600/newprod -c 'select count(*) from customers'   # -> 5",
   "leaked": true,
   "fix": "Three changes, in order of how much they buy.\n(1) internal/pg/source.go: make the cluster identity degrade field by field instead of all-or-nothing. sqlClusterID is one SELECT, so a permission error on pg_postmaster_start_time() kills the whole row and the identity becomes \"\" — the state the gate reads as 'unknown' and then resolves by endpoint spelling, which is exactly what an alias changes. Wrap each field so an unreadable one is the empty string rather than an error, e.g. compute the start time in a scalar subquery guarded by has_function_privilege('pg_postmaster_start_time()','EXECUTE') (and the same for pg_control_system), so a role that lost one function still contributes the fields it can read. On this cluster the target side could read everything; only the source side collapsed.\n(2) internal/pg/target.go, Target.Gate: the last arm of the SameCluster switch (line 263, targetRef.SameCluster(source)) must not be allowed to answer 'different cluster' from an endpoint spelling when both catalog identities are unknown. When clusterUnknown() is true, SameCluster should be reported as unknown-and-treated-as-true for the purposes of the warning and of T-0184's headless refusal — i.e. warn 'the target may be on the source's own cluster: neither server would identify itself' — rather than silently false. A false negative here is a production write; a false positive is one extra warning line. The same reasoning already justifies the sameCatalog fail-closed arm, but that arm is gated on currentDB == source.Database and so covers only the case where the two databases share a name; the mid-migration case this attempt used (app -> newprod on one cluster) is outside it.\n(3) Close the precondition: add `GRANT EXECUTE ON FUNCTION pg_control_system() TO lazyslice_ro;` to the recommended-role snippet in ARCHITECTURE.md §9 and in internal/core/run.go's CodeRoleWritable message (round-2 fix item 3, not yet landed). With system_identifier readable on the source side, neither this variant nor the original R2-06 has anything to work with.\nRegression test: internal/pg/gate_integration_test.go should assert that a source whose role can execute neither pg_control_system nor pg_postmaster_start_time still yields SameCluster == true for a target on that same cluster reached over a different transport.",
   "attacker": "attack:2:r3"
  },
  {
   "attack": "Defeat the run lease with a second concurrent run against a target a first run already passed the gate on (surface item: run lease). Run A is stalled mid-run by holding ACCESS EXCLUSIVE on the source table after it has taken the lease; run B is launched at the same target.",
   "reproduction": "psql postgres://postgres@127.0.0.1:15600/app -c 'begin; lock table customers in access exclusive mode; select pg_sleep(25); commit;' &\n$B/lazyslice --source 'postgres://reader:pw@127.0.0.1:15600/app' --target 'postgres://postgres@127.0.0.1:15600/devcopy' --yes --no-config -n 5 &   # run A, stalls on the source lock\nsleep 6\n$B/lazyslice --source 'postgres://reader:pw@127.0.0.1:15600/app' --target 'postgres://postgres@127.0.0.1:15600/devcopy' --yes --no-config -n 5   # run B\n#  x the target devcopy is held by ac04bcfc-...: one lazyslice run writes a target at a time\n#  lazyslice: target.refused.lease_held\n#  exit=4",
   "leaked": false,
   "fix": "None. The lease is taken before the gate's first probe (internal/core/run.go's acquireLease, line ~805) and run B refused with target.refused.lease_held at exit 4, writing nothing. Behaviour matches ARCHITECTURE.md §11.2 and round 1's finding 15.",
   "attacker": "attack:2:r3"
  },
  {
   "attack": "Defeat the per-table lock-and-recheck (T-0130) with a concurrent writer: let run A pass the emptiness gate, stall it on the source, then insert a row into the target's customers table before the loader reaches its DROP, so that a third party's data is dropped on a verdict that is by then a remembered fact.",
   "reproduction": "# run A stalled as in the previous attempt, gate already passed on an empty devcopy\npsql postgres://postgres@127.0.0.1:15600/devcopy -c \"INSERT INTO customers(email,full_name,phone,ssn) VALUES ('someone-elses-work@dev.example','Other Work','+16505551212','999-99-9999')\"\n# release the source lock, let run A continue:\n#  dropping public.orders in the target\n#  dropping public.customers in the target\n#  x public.customers was approved empty and now holds 1 rows: the target changed after it was approved, and nothing was dropped\n#  lazyslice: load.refused.target_changed  (exit 4)\npsql -tA postgres://postgres@127.0.0.1:15600/devcopy -c 'select email from customers'   # -> someone-elses-work@dev.example, still there",
   "leaked": false,
   "fix": "The recheck holds: the row survived and the run refused at exit 4 with load.refused.target_changed. One thing to note rather than a leak — the loader drops table by table and had already dropped public.orders before it rechecked public.customers, so the refusal leaves the target partially dropped (here orders was empty, so nothing was lost, but a concurrent writer who had populated orders and customers would lose orders). Worth a tracker item: either take the recheck for every table in the plan before the first DROP, or say in the refusal message which tables were already dropped so the operator knows the target is half-taken-apart. Filed as an observation only; I changed no repo file.",
   "attacker": "attack:2:r3"
  },
  {
   "attack": "Get the tool to hold and use write privileges on the source: give the source role INSERT/UPDATE/DELETE and smuggle a data-modifying call into the one place the operator's own SQL reaches the source — --where, using a VOLATILE plpgsql function that INSERTs into an audit table.",
   "reproduction": "psql postgres://postgres@127.0.0.1:15600/app -c 'CREATE TABLE audit_hits(note text)' \\\n  -c 'GRANT INSERT,UPDATE,DELETE ON customers TO reader; GRANT INSERT ON audit_hits TO reader;' \\\n  -c \"CREATE FUNCTION pwn(t text) RETURNS boolean LANGUAGE plpgsql VOLATILE AS \\$\\$ BEGIN INSERT INTO audit_hits VALUES (t); RETURN true; END \\$\\$\" \\\n  -c 'GRANT EXECUTE ON FUNCTION pwn(text) TO reader'\n$B/lazyslice --source 'postgres://reader:pw@127.0.0.1:15600/app' --target 'postgres://postgres@127.0.0.1:15600/devcopy' \\\n  --yes --no-config -n 5 --where \"pwn('write-from-where')\"\n#  ! the role reader can write to 2 table(s) in app - recommend a read-only role: CREATE ROLE lazyslice_ro ...\n#  x lazyslice failed for a reason it has no code for; run with --debug\n#  lazyslice: cannot execute INSERT in a read-only transaction (SQLSTATE 25006)   exit=1\npsql -tA postgres://postgres@127.0.0.1:15600/app -c 'select count(*) from audit_hits'   # -> 0",
   "leaked": false,
   "fix": "The source write was blocked: every source statement runs inside BEGIN READ ONLY REPEATABLE READ (internal/pg/pg.go's sqlBeginReadOnly), the writable-role warning fired with the recommended-role snippet, and zero rows reached the source. Two defects worth fixing anyway, neither a leak: (a) the refusal is uncoded — 'lazyslice failed for a reason it has no code for; run with --debug' at exit 1, where a SQLSTATE 25006 raised by the operator's own --where is a predictable, nameable failure and should be an event.Code in internal/event/catalogue.yml with a message that points at --where; (b) the recommended-role snippet printed here is the one that should also carry `GRANT EXECUTE ON FUNCTION pg_control_system() TO lazyslice_ro;` (round-2 fix item 3), which is the precondition of the leaking variant above.",
   "attacker": "attack:2:r3"
  },
  {
   "attack": "Replay case 1/7 (T-0130): target race — a guard holds ACCESS EXCLUSIVE lock on the source table through the plan/extract gate; once the target-write gate completes, insert a decoy row 999 into the target, then release the guard and let load proceed. Claim: refused at exit 4, target keeps only the decoy row.",
   "reproduction": "python3 docs/reviews/2026-09-09/evidence/target_race.py (builds /tmp/lazyslice-review from current tree, spins a disposable postgres:18 container, tears it down in finally)",
   "leaked": false,
   "fix": "MATCHES CLAIM. Result: {\"exit\": 4, \"inserted_after_gate\": \"1\", \"remaining_after_load\": \"1\", \"target_ids\": \"999\"}. lazyslice refused at exit 4 and the target still holds only the interloper row 999 — the source row never landed. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Vary case 1: rerun target_race.py verbatim a second time (same schema, same timing) to confirm the refusal is not timing-flaky.",
   "reproduction": "python3 docs/reviews/2026-09-09/evidence/target_race.py (run twice in immediate succession)",
   "leaked": false,
   "fix": "MATCHES CLAIM on rerun as well — exit 4, target_ids stayed \"999\" both times. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Replay case 2/7 (T-0137): an email-shaped JSON object key (`{\"canary.person@example.org\": \"ok\"}`) reached via jsonb_object_keys() on the target.",
   "reproduction": "python3 docs/reviews/2026-09-09/evidence/probes.py json_keys",
   "leaked": false,
   "fix": "MATCHES CLAIM. Target key came back masked (a fabricated address), not the source key. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Vary case 2: the same JSON-key probe but as a value inside a nested object (not a top-level key), plus a second row so more than one key/row is checked.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3 && docker run postgres:18; CREATE TABLE items(id int PRIMARY KEY, payload jsonb); INSERT 2 rows with a nested {\"meta\":{\"grace.hopper@navy.example.org\":\"x\"}} object; run /tmp/lazyslice-review --root public.items --take 20 --yes --no-config; SELECT payload FROM items on target",
   "leaked": false,
   "fix": "Both rows' nested keys came back masked, not verbatim (informal spot check while wiring the base case; not separately archived). No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Replay case 3/7 (T-0134): a column DEFAULT holding a canary email, read back from pg_attrdef, plus the row value.",
   "reproduction": "python3 docs/reviews/2026-09-09/evidence/probes.py ddl_default",
   "leaked": false,
   "fix": "MATCHES CLAIM. Target row value and pg_get_expr(adbin,adrelid) default both came back masked ('ivan.conti@example.net' and \"'emeka.jelinek@example.com'::text\" — both fabricated, not the canaries). No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Vary case 3: a second masked-default column of a different type family (an address-shaped default instead of email) on the same table.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/natid-test; CREATE TABLE items(id int PK, email text DEFAULT 'ddl.canary@example.org', addr text DEFAULT '1742 Kestrel Hollow Lane, Ashford VT 05024'); run and read pg_attrdef",
   "leaked": false,
   "fix": "Both defaults masked; the address default was rewritten to a fabricated address, not left verbatim. No fix needed. (Folded into the run6/run6b catalog tests below rather than logged separately.)",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Replay case 4/7 (T-0131): one email hidden among 19 'ok' rows (sparsity), counted back on target by exact source value.",
   "reproduction": "python3 docs/reviews/2026-09-09/evidence/probes.py sparse_email",
   "leaked": false,
   "fix": "MATCHES CLAIM. count(*) WHERE v='sparse.canary@example.org' on the target is 0. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Vary case 4: two sparse hits in the same 20-row column instead of one, at different offsets.",
   "reproduction": "CREATE TABLE items(id int PK, v text); INSERT SELECT i, CASE WHEN i IN (1,15) THEN 'sparse'||i||'.canary@example.org' ELSE 'ok' END FROM generate_series(1,20) i; run and count matches on target",
   "leaked": false,
   "fix": "Both sparse rows masked; 0 matches for either canary value on target. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Replay case 5/7 (T-0133): verify fails on an unmasked email that classify missed; check the target is emptied and lazyslice_meta carries a failed marker.",
   "reproduction": "python3 docs/reviews/2026-09-09/evidence/probes.py verify_failure, plus a direct rerun via a custom harness (run_case.py) checking `SELECT to_regclass('items')` and `SELECT status FROM lazyslice_meta ORDER BY started_at DESC LIMIT 1`",
   "leaked": false,
   "fix": "MATCHES CLAIM (with a caveat on exit code): the second net catches column `v` as email (classify itself said \"no name or value signal\" for `v`), the run refuses at exit 9 (verify.refused.second_net, not exit 4 — exit 4 is specific to the target-race case, not this one), the target's `items` table is dropped (to_regclass returns null) and lazyslice_meta.status = 'failed'. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Vary case 5: two independently-missed strong-signal columns in the same table instead of one, to confirm verify reports and the target still empties cleanly.",
   "reproduction": "Folded into the natid-test session while building other cases rather than run as its own harness; not separately re-verified this round — flagging as untested rather than claiming a result.",
   "leaked": false,
   "fix": "Not independently re-verified this round; the single-column case (above) is solid evidence the mechanism works. No fix needed based on available evidence.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Replay case 6/7 (T-0132): a credential-shaped token shared via FK (tokens.token <- items.token) must be masked identically on both sides of the reference.",
   "reproduction": "python3 docs/reviews/2026-09-09/evidence/probes.py fk_masker",
   "leaked": false,
   "fix": "MATCHES CLAIM. Both tokens.token and items.token came back as the same fabricated value ('lazyslice-invalid-snvurpjganvkv') on the target — the FK-connected equality group still shares one masker output. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Vary case 6: a self-referencing FK (items.parent_token -> items.token) instead of a two-table FK, to see if the equality-group logic generalises.",
   "reproduction": "Not run this round — time budget spent on the round-2 replays instead; flagging as an untested variant rather than claiming a result.",
   "leaked": false,
   "fix": "Not run. No claim made.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Replay case 7/7 (T-0131): an ambiguous 'thing_type' polymorphic-log column holding an email-shaped canary, must be masked despite the column name giving no personal-data hint by itself.",
   "reproduction": "python3 docs/reviews/2026-09-09/evidence/probes.py polymorphic_log",
   "leaked": false,
   "fix": "MATCHES CLAIM. Target thing_type came back as a fabricated email ('javier.nakamura@example.net'), not the canary. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Vary case 7: sslmode/certificate-path round-trip (T-0135's claim) — write lazyslice.yml on a first run with sslmode=prefer&connect_timeout=7 on the source and sslmode=prefer on the target, confirm both non-secret transport params are preserved in the emitted config's `params` map rather than dropped.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/sslmode-test; docker run postgres:18; LAZYSLICE_SECRET=... /tmp/lazyslice-review --source 'postgres://postgres@127.0.0.1:<port>/s_ssl?sslmode=prefer&connect_timeout=7' --target 'postgres://postgres@127.0.0.1:<port>/t_ssl?sslmode=prefer' --root public.items --take 20 --yes; cat lazyslice.yml",
   "leaked": false,
   "fix": "MATCHES CLAIM. lazyslice.yml's source.params holds {connect_timeout: \"7\", sslmode: prefer} and target.params holds {sslmode: prefer} — neither was dropped, matching T-0135's dsn.Ref allowlist fix. Did not go on to build a TLS interception test (out of scope for this pass); the round-trip into the committed config is what T-0135 actually promised and it holds.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Round2 item 0 (A2) — six columns with no name-rule crafted to miss every regex/parser: full name in `label`; obfuscated email in `handle2`; phone-in-words in `contact`; interleaved-spelling UK NI number in `ident`; dotted payment card in `pan`; wordy address in `whereabouts`. Round2 found only `ident` still leaked (60/60 hits verbatim).",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/natid-test; CREATE TABLE records(id int PK, label text, handle2 text, contact text, ident text, pan text, whereabouts text); insert 3 rows matching round2's shapes; run /tmp/lazyslice-review --root public.records --take 10 --yes --no-config; SELECT * FROM records on target",
   "leaked": true,
   "fix": "PARTIALLY FIXED, ONE NEW LEAK FOUND. `ident` (the round2 finding) is now fixed: classify reports \"2/3 samples parse as national identifiers ... masked as free_text\" and the target holds fabricated garble text, not the source values — T-0187's row-path wiring works. label/handle2/pan/whereabouts also masked correctly. But `contact` (the phone spelled entirely in words, e.g. 'oh seven seven oh oh one two three four') crosses completely verbatim, exit 0, classify says \"no name or value signal\". Root cause, isolated to a single-column table to rule out interference (contact-only table, same result): internal/classify's free_text rule is `{pipeline.CatFreeText, phraseProse, false, func(d *textsig.Dict, s string) bool { return d.Prose(s) }}` and `Dict.Prose` (internal/textsig/dict.go:195) requires `d.ContainsName(s)` on top of the >=6-word floor — so prose with zero digits and zero dictionary-name words (a phone spelled entirely as number-words) fails Prose and there is no phone-in-words validator at all, so the column falls to `none` and copies verbatim. Fix: either give Prose (or a new rule) a shape check for \"mostly number-words\" (a small word-to-digit dictionary: zero..nine, oh, double, etc.), or loosen the free_text gate so a long run of purely lowercase words with no punctuation and no stopword-sentence shape also qualifies, corroborated by the neighbouring-column rule the way label/whereabouts already are. Not filed as a tracker task per this task's 'do not change any repo file' instruction; reporting only.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Variant of the contact finding: same phone-in-words value, but now with a column NAME that carries no personal-data hint changed to `contact_name` on a table that also has an unrelated `note` column, to see whether either the name-pattern rule or the neighbouring-column rule would rescue it if the column were named more suggestively.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/natid-test; CREATE TABLE records(id int PK, contact text); INSERT 3 rows with the exact same three phone-in-words values as the A2 attack, run in isolation, SELECT * on target",
   "leaked": true,
   "fix": "Same leak, isolated: all 3 phone-in-words values crossed verbatim (\"oh seven seven oh oh one two three four\", etc.), confirming it is not an artefact of the other five A2 columns interacting with the neighbouring-column rule — the gap is Prose's name-word requirement, described above.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Round2 item 1 (A6) — plain, unobfuscated national identifiers (hyphenated SSN, plain UK NI number) in columns whose names miss the rule pack (`ref_code`, `ni_ref`), in a table with no other personal column so the neighbouring-column rule cannot fire. Round2 found both crossed verbatim.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/natid-test; CREATE TABLE benefit_claims(id int PK, ref_code text, ni_ref text); INSERT 5 rows with '078-05-112N' SSNs and 'AB12345ND' NI numbers; run /tmp/lazyslice-review --root public.benefit_claims --take 10 --yes --no-config; SELECT id,ref_code,ni_ref FROM benefit_claims on target",
   "leaked": false,
   "fix": "FIXED. classify reports \"5/5 samples parse as national identifiers; no name signal\" for both columns and both are masked to fabricated digit strings on the target, not the source values — T-0187's row-path wiring (textsig.ValidNationalID/ValidNationalIDStructured added to internal/classify's validators list) closes this. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Round2 item 2 (A7) — the same national_id gap reached through a text[] array (`id_list`), a plain SSN in a column named `code`, and an SSN buried at depth 3 inside a jsonb-shaped text column (`doc`). Round2 found all three crossed.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/natid-test; CREATE TABLE audit_docs(id int PK, doc text, id_list text[], code text); insert 2 rows with a nested-JSON SSN, an array of NI numbers, and a plain SSN in `code`; run and read back doc/id_list/code on target",
   "leaked": false,
   "fix": "FIXED. code and id_list are both masked (fabricated digit strings; id_list reports \"classified on the element type text\"), and doc — classified as \"looks like secrets\" — is replaced wholesale with a masked-secret placeholder ($lazyslice$invalid) rather than left readable. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Round2 item 3 (A9b) — the Luhn-valid card removed so the second net's numeric-family entry can't fire on it; what remains is an SSN stored as bigint (`taxref`) and a phone stored as numeric (`msisdn`), with NO other personal-data column in the table to corroborate. Round2 found taxref crossed verbatim.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/natid-test; CREATE TABLE payroll(id int PK, taxref bigint, msisdn numeric); INSERT 4 rows (78051120..78051123, phone-shaped numerics); run and SELECT id,taxref,msisdn on target",
   "leaked": true,
   "fix": "STILL LEAKS in this shape, but this is a known, documented, intentionally scoped-out residual, not a regression. msisdn is masked (name matches phone), taxref crosses verbatim exit 0 (\"no name or value signal\"). internal/classify/classify.go's own comment above the national_id entries (lines 355-370) states explicitly that a bigint SSN with no name hit is decided `none` by classify by design, and is meant to be caught instead by internal/verify's digits-family entry (T-0187) — but that entry only refuses with corroboration (a name-pattern hit on the column itself, or a certain/likely personal column elsewhere in the table), per testdata/regressions/020-ssn-stored-as-bigint.sql's own header. My payroll table has no corroborating column (msisdn is masked via a NAME match, not a certain/likely VALUE match, so it doesn't count). Confirmed the mechanism does work when corroboration exists: running the official fixture testdata/regressions/020-ssn-stored-as-bigint.sql (taxref bigint + email) verbatim against the current binary produces \"lazyslice: verify.refused.second_net: ... on public.reg020_payroll.taxref: national_id\" — a correct refusal. So this is the accepted, already-filed residual the T-0189 postmortem calls out (\"Closing the classify-side gap ... is a wider change than this task's paths reach and is reported separately\"), not a fresh finding.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Variant of A9b: the official 020 fixture itself (taxref bigint + email, non-sequential SSN-shaped values), to confirm the corroboration path actually closes the gap rather than just being aspirational in a comment.",
   "reproduction": "docker exec -i <container> psql -U srcuser -d srcdb -v ON_ERROR_STOP=1 < testdata/regressions/020-ssn-stored-as-bigint.sql; run /tmp/lazyslice-review --root public.reg020_payroll --take 10 --yes --no-config",
   "leaked": false,
   "fix": "CONFIRMED FIXED for the corroborated case: refuses at verify.refused.second_net on taxref:national_id, matching the fixture's own documented expectation (exit 9). The residual is specifically the no-corroboration case shown above.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Round2 item 4 (A10) — a person's full name the dictionary does not carry (Khmer/Lao/Amharic transliterations: Sokhem Chanthavy, Bounmy Phimmasone, Tesfaye Gebreselassie, Rithy Sopheak, Khamla Vongsa), in a column called `label` (no name-rule match), table has no other personal column. Round2 found all five crossed verbatim.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/natid-test; CREATE TABLE cases(id int PK, label text); INSERT the 5 names; run /tmp/lazyslice-review --root public.cases --take 10 --yes --no-config; SELECT * FROM cases on target",
   "leaked": true,
   "fix": "STILL LEAKS, unchanged from round2. All 5 names crossed verbatim, classify reports \"no name or value signal\" for `label`. No tracker task claims to have closed this; it remains the residual THREAT_MODEL.md T1 itself names as open (a name dictionary can never be complete) and is architecturally the hardest of the round2 findings — there is no value-shape signal to catch a name that isn't in the dictionary and isn't in a name-patterned column.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Variant of A10: same five names, but with the column renamed to `contact_name` (a name that matches the column-name rule pack) instead of `label`, to confirm the gap really is name-dictionary-specific and not a wider name-masking failure.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/natid-test; CREATE TABLE cases(id int PK, contact_name text, note text); INSERT 2 of the same names; run and SELECT * on target",
   "leaked": false,
   "fix": "Confirms the root cause precisely: with a name-signalling column NAME, classify masks the column regardless of whether the specific values are in the dictionary (\"name matches person_name\", target holds fabricated names). The gap is narrowly: (no column-name signal) AND (value not in the name dictionary) AND (no other personal column in the table to trigger the neighbouring-column rule).",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Round2 item 5 — wrong-target-alias: defeat ARCHITECTURE.md §9 rule 1 by reaching one production cluster over two transports (TCP vs the same server's Unix socket), since inet_server_addr()/inet_server_port() are NULL over a socket, giving the SAME cluster two different reported identities. Round2 found the socket-addressed target skipped the same-cluster warning entirely and loaded 5 rows with exit 0.",
   "reproduction": "initdb+pg_ctl a real local cluster on 127.0.0.1:15700 with both a TCP listener and a Unix socket dir; create app (with customers: email/full_name/phone/ssn) and an empty newprod db; control run with --target over TCP; attack run with --target 'postgres://postgres@/newprod?host=<sockdir>&port=15700' (same cluster, socket transport)",
   "leaked": false,
   "fix": "FIXED. Both the TCP and the socket-addressed target now print \"! target newprod is on the same cluster as the source: this run writes to the source's own server\" and (on rerun) \"newprod carries lazyslice's own marker for this source\" — matches T-0190 (cluster identity uses values constant across sessions on the cluster, not inet_server_addr()/port()). No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Variant of the wrong-target-alias attack: address the same cluster a third way — via the hostname `localhost` instead of `127.0.0.1` or the socket path — another common 'same server, different string' bypass.",
   "reproduction": "same cluster as above; --target 'postgres://postgres@localhost:15700/newprod'",
   "leaked": false,
   "fix": "Also caught: \"! target newprod is on the same cluster as the source\" fires identically. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Round2 item 6 — catalog residue via CHECK constraints carrying non-'strong' personal data (a person's full name, a postal address, a special-category/health sentence) in `pg_get_constraintdef`. Round2 found all three crossed verbatim (strongCatalogHit/strongHit only ran the five strong validators).",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/catalog-test; two postgres:16 containers (source/target); CREATE TABLE customers(id, full_name, street_address, notes) with CHECK constraints embedding 'Aurelio Nakamura-Okonkwo' (name), '1742 Kestrel Hollow Lane, Ashford VT 05024' (address), 'Priya Raghunathan disclosed her HIV diagnosis on 2019-04-02' (health/special-category); run /tmp/lazyslice-review --root public.customers --take 10 --yes --no-config against each constraint isolated in its own table",
   "leaked": true,
   "fix": "PARTIALLY FIXED, ONE LEAK REMAINS. The name-only CHECK and the address-only CHECK both now correctly REFUSE at exit 13 (target.schema.literal_not_rewritable, naming person_name/address) — matches T-0189's widening of strongHit/strongCatalogHit to every validator over string literals. But the health/special-category-only CHECK (ck_notes_free, no name or address literal in the same constraint to trip a refusal first) runs to completion at exit 0, and `SELECT pg_get_constraintdef(oid) ... WHERE conname='ck_notes_free'` on the target returns the sentence verbatim: \"CHECK (((notes IS NULL) OR (notes <> 'Priya Raghunathan disclosed her HIV diagnosis on 2019-04-02'::text)))\". This matches the open tracker task T-0198 ('special_category has no value validator; a digit/name-free special-category sentence still crosses unseen') and the T-0189 postmortem's own admission ('the special-category sentence is still only caught by accident'). Not filed as a new tracker task since T-0198 already covers it and this task forbids repo writes; reporting as confirmation T-0198 is still open and live.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Variant of the CHECK-constraint residue: the same three-CHECK schema combined on one table (name + address + health, as in the original round2 attack), to see whether the refusal on name/address at least prevents the health sentence from loading even transiently.",
   "reproduction": "same containers; CREATE TABLE customers with all three CHECK constraints plus a fourth pattern-operand email regex CHECK together; run once",
   "leaked": false,
   "fix": "Combined, the run refuses immediately on ck_addr_block (exit 13) before touching the target at all — target has zero tables afterward, so the health-sentence CHECK never gets a chance to load in this combined shape. The isolated single-constraint test above is what actually demonstrates the T-0198 gap; in practice a table this red-teamy (three CHECK constraints, all with the same literal-holder value) is unlikely, and a real health-only CHECK with no accompanying name/address CHECK is the realistic exploit shape.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Round2 item 7 — catalog residue via table CHECK-style partial index predicates (`CREATE INDEX ... WHERE full_name = '...'` / `WHERE street_address = '...'`), since internal/plan read no pg_index at all (T-0163) and the post-load catalog pass only ran the five strong validators. Round2 found both predicates crossed verbatim.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/catalog-test; CREATE TABLE customers(id, full_name, street_address) with CREATE INDEX idx_cust_vip ... WHERE full_name = 'Aurelio Nakamura-Okonkwo' and CREATE INDEX idx_cust_addr ... WHERE street_address = '1742 Kestrel Hollow Lane, Ashford VT 05024', tested both combined and each isolated to its own table; run /tmp/lazyslice-review --root public.customers --take 10 --yes --no-config; read back pg_get_indexdef(indexrelid) WHERE indpred IS NOT NULL on target",
   "leaked": false,
   "fix": "FIXED. Both the name-predicate index (isolated) and the address-predicate index (isolated, and combined) now refuse at exit 13 (target.schema.literal_not_rewritable, naming the index and the parsed category) before the target is touched — matches T-0163/T-0189's plan reading pg_index partial-index predicates. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Variant of the partial-index attack: an index predicate that itself uses a pattern operator (WHERE full_name ILIKE '%Aurelio Nakamura-Okonkwo%') instead of plain equality, to see whether the pattern-operand exemption (which round2 item 9 showed bypassed detection entirely) also lets a partial-index predicate slip through.",
   "reproduction": "Not run this round — time budget spent on the higher-priority replays; flagging as an untested variant.",
   "leaked": false,
   "fix": "Not run. No claim made; worth a follow-up given item 9's fix specifically targeted CHECK/generated-expression pattern operands and the fixture set (T-0189) doesn't explicitly mention pattern-operand partial-index predicates.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Round2 item 8 — catalog residue via the four object kinds the 2026-09-15 A4b/A11/A12 amendment added reads for but not validators for: enum labels, domain CHECK, domain DEFAULT, generated-column expressions, using non-strong categories (a name in an enum label, an address in a domain CHECK+DEFAULT, a health sentence baked into a generated column). Round2 found all four crossed verbatim.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/catalog-test; CREATE TYPE case_owner AS ENUM ('Aurelio Nakamura-Okonkwo','unassigned'); CREATE DOMAIN home_addr AS text CHECK (VALUE <> '...Ashford VT 05024') DEFAULT '...Ashford VT 05024'; CREATE TABLE cases(... owner case_owner, addr home_addr, summary text GENERATED ALWAYS AS (addr || ' :: Priya Raghunathan HIV disclosure 2019-04-02') STORED); tested combined and each object isolated; run /tmp/lazyslice-review --root public.cases --take 10 --yes --no-config; read pg_enum/pg_constraint(conrelid=0)/pg_type.typdefault/pg_attrdef(attgenerated<>'') on target",
   "leaked": false,
   "fix": "FIXED for enum label and domain CHECK/DEFAULT: both refuse at exit 13 (target.schema.type_literal, naming the type and category, and pointing at --allow-type-literal as the reasoned opt-out — matches the separately-verified T-0186 opt-out mechanism). The generated-column expression carrying only the health sentence (no address literal) does NOT refuse at plan time (that pass apparently doesn't parse generated-expression literals the way it parses CHECK/domain/enum literals), but the run still refuses at LOAD time via the second net (verify.refused.second_net on `summary`: address) because the second net's address validator has a broad 'mixed digits and words' false-positive net that happens to catch the generated text; the target is dropped afterward (0 tables remain) exactly as T-0133 describes. So no persistent leak was observed, but the postmortem for T-0189 itself flags this as 'caught by accident' rather than by a real special-category validator (T-0198 is the same open gap as case 6) — a differently-shaped generated expression (no digits, no address-like fragment) could plausibly evade the accidental catch. Not independently confirmed to leak in my testing; flagging the fragility rather than a confirmed leak.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Variant of the enum/domain/generated attack: swap the sensitive literal into the generated-column expression only (no address portion at all — 'case #' || code || ' :: Priya Raghunathan disclosed her HIV diagnosis...'), to see if removing the accidental address-shape trigger lets the health sentence through fully.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/catalog-test; CREATE TABLE cases(id, code integer, summary text GENERATED ALWAYS AS ('case #'||code||' :: Priya Raghunathan disclosed her HIV diagnosis on 2019-04-02') STORED); run and check target",
   "leaked": false,
   "fix": "Still caught — the second net's address validator's 'mixed digits and words' shape check fired on the generated string even without an address-shaped fragment in it (the whole sentence has both digits, from 'case #100', and words), refusing at exit 9 and dropping the target to 0 tables. This is still the 'caught by accident, not by design' pattern the T-0189 postmortem names, so I would not treat it as durably fixed, but it did not leak in either variant I tried.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "Round2 item 9 — bypass the strong-validator net for a genuinely strong category (email) by putting the value on the right-hand side of a pattern operator (CHECK (email !~ '^ceo@bigcorp\\.example$')), which internal/pipeline/ddlliteral.go's afterPatternOperator marked as a shape (never a value) for BOTH detection and rewriting. Round2 found the regex form crossed at exit 0 while the semantically identical `<>` form correctly refused at exit 13.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-3/catalog-test; CREATE TABLE customers(id, email, CONSTRAINT ck_email_regex CHECK (email !~ '^ceo@bigcorp\\.example$')); run, then add CONSTRAINT ck_email_plain CHECK (email <> 'ceo@bigcorp.example') to the same table and rerun on a reset target for direct comparison",
   "leaked": false,
   "fix": "FIXED. Both forms now refuse identically at exit 13 (target.schema.literal_not_rewritable, naming ck_email_regex / ck_email_plain and 'email' as the parsed category) — matches T-0189's stated fix that a pattern-operand literal is exempt from rewriting only, never from detection. No fix needed.",
   "attacker": "attack:4:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 1 (A2) — values crafted to miss every regex/parser in columns with no name rule: full name in `label`, obfuscated email in `handle2`, spelled-out phone in `contact`, spaced/spelled UK NI number in `ident`, dot-separated PAN in `pan`, wordy address in `whereabouts`.",
   "reproduction": "docker run -d --name rt3-src -e POSTGRES_PASSWORD=srcpw -e POSTGRES_USER=srcuser -e POSTGRES_DB=srcdb -p 55631:5432 postgres:16\ndocker run -d --name rt3-tgt -e POSTGRES_PASSWORD=tgtpw -e POSTGRES_USER=tgtuser -e POSTGRES_DB=tgtdb -p 55632:5432 postgres:16\ncd <repo> && go build -o /private/tmp/claude-501/lazyslice-scratch/redteam-2/lazyslice ./cmd/lazyslice\ncd /private/tmp/claude-501/lazyslice-scratch/redteam-2 && ./run.sh a2 a2_values.sql --root public.records --take 60\n./check.sh 'AB nine eight seven six five four D' 'QQ one two three four five six C' 'Grace Hopper' 'grace.hopper' '4539.1488' 'Rowan House' 'oh seven seven oh oh'\n(harness: /private/tmp/claude-501/lazyslice-scratch/redteam-2/run.sh, check.sh, a2_values.sql)",
   "leaked": false,
   "fix": "NOT LEAKED. Exit 0 but every needle is 0 hits in the target dump. `records.ident` is now reported as '30/60 samples parse as national identifiers; a strong validator hit below the category threshold: masked as free_text' — the national_id validator that round 2 found registered nowhere on the row path is now in internal/classify/classify.go:371-372 (ValidNationalIDStructured / ValidNationalIDChecksumOnly) and internal/verify/validators.go:263-264. The other five columns are caught as before. Commit 25cc069 closes this. No fix owed.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 2 (A6) — PLAIN, unobfuscated national identifiers in columns whose names miss the rule pack, in a table with no other personal column: `ref_code` holds hyphenated US SSNs (078-05-NNNN), `ni_ref` holds UK NI numbers (ABNNNNNND).",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2 && ./run.sh a6 a6_natid.sql --root public.benefit_claims --take 300\n./check.sh '078-05-' 'AB100' 'AB1000'\ndocker exec rt3-tgt psql -U tgtuser -d tgtdb -c 'select id,ref_code,ni_ref from benefit_claims order by id limit 5'",
   "leaked": false,
   "fix": "NOT LEAKED. Exit 0, 0 hits for all three needles. Both columns now read '200/200 samples parse as national identifiers; no name signal' and are masked. Same fix as the attempt above (commit 25cc069). No fix owed.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 3 (A7) — the national_id gap through three other carriers: a `text[]` of UK NI numbers (`id_list`), a plain SSN in a column called `code`, and a JSON document in a `text` column whose only personal datum is an SSN at depth 3 (`doc`).",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2 && ./run.sh a7 a7_natid_nested.sql --root public.audit_docs --take 300\n./check.sh '078-05-' 'AB100' 'JG200' '123-45-'\ndocker exec rt3-tgt psql -U tgtuser -d tgtdb -At -c 'select doc,id_list,code from audit_docs order by id limit 2'",
   "leaked": false,
   "fix": "NOT LEAKED. Exit 0, 0 hits for all four needles. `id_list` is now '400/400 samples parse as national identifiers ... classified on the element type text' and `code` '200/200 samples parse as national identifiers' — both masked, so the array path and the scalar path are both covered. Residual worth noting, not a leak: `doc` is still masked only because textsig.LooksSecret's entropy guess fires ('200/200 samples look like secrets') and it is replaced with the fixed literal `$lazyslice$invalid`, which destroys the JSON so a developer gets an unparseable document. That is the same luck round 2 flagged; it is unchanged and still deserves the JSON-aware masker rather than the credential literal.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 4 (A9b) — an SSN stored as `bigint` (`payroll.taxref`) and a phone stored as `numeric` (`payroll.msisdn`), with no Luhn-valid card to fire the second net's other numeric entry.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2 && ./run.sh a9b a9b_numeric.sql --root public.payroll --take 300\n./check.sh '78051' '44770090'\ndocker exec rt3-tgt psql -U tgtuser -d tgtdb -c 'select id,taxref,msisdn from payroll order by id limit 4'\n# and the post-load net on its own:\ncd /private/tmp/claude-501/lazyslice-scratch/redteam-2/runs/a9b && ../../lazyslice verify --source 'postgres://srcuser:srcpw@127.0.0.1:55631/srcdb?sslmode=disable' --target 'postgres://tgtuser:tgtpw@127.0.0.1:55632/tgtdb?sslmode=disable' --root public.payroll --take 300 --yes",
   "leaked": true,
   "fix": "STILL LEAKED. Exit 0. `msisdn` is saved by its name matching phone, but `public.payroll.taxref` is reported 'no name or value signal' and all 300 SSNs cross verbatim — 300 hits for '78051' in the target dump, target rows read `1 | 78051001`. `lazyslice verify` on the loaded target also exits 0, so the second net is silent too. Nothing leaks into lazyslice.yml, the transcript, --json or the temp files: this is a target-rows-only leak.\n\nRoot cause, verified, and it is NOT the one round 2 predicted. The two-part fix round 2 asked for did land: internal/verify/validators.go:346-349 now has a `digits: true` national_id entry, and textsig.ValidNationalIDDigits('78051001') returns true (probed against internal/textsig). Two guards on that one entry both hold it back:\n  1. `sequenceExempt: true` plus digitRange.dense() (internal/verify/secondnet.go:349, :259-277). taxref's values are 78051001..78051300 — 300 values in a span of 300 — so the column reads as a generated sequence and the entry is skipped before its ratio is ever scored. Any real payroll whose national-ID block is issued contiguously has exactly this shape.\n  2. `requiresCorroboration: true` (secondnet.go:357, corroborated() at :298-301) needs `NameMatchedNationalID` or `TableHasLikelyPersonalColumn`. `msisdn` matched phone BY NAME ONLY with no value samples, and internal/classify/classify.go:848-849 gives a name-only, sample-less match `ConfPossible` — masked, but strictly below the `ConfLikely` floor internal/classify/classify.go:1178 counts for TableHasLikelyPersonalColumn. So a table whose only other personal column was decided on its name alone corroborates nothing.\n\nFix. (a) TableHasLikelyPersonalColumn is the wrong predicate for corroboration: a column lazyslice itself chose to MASK is a proven personal neighbour whatever confidence carried it there. Count `w.d.Masked` (or `Confidence >= ConfPossible` restricted to identifiesAPerson categories) in the neighbour tally at classify.go:1178, not `>= ConfLikely`; the ConfLikely floor belongs to the neighbouring-column raising rule, not to this signal, and reusing one number for both is what made a masked phone column invisible. (b) The dense-range exemption must not apply once corroboration is present: at secondnet.go:349, skip on `dense` only when `!corroborated`. A dense block of nine-digit values in a table that already holds a masked personal column is the leak this entry exists for, and the 'it is a generated sequence' reading is the weaker of the two. (c) Add the missing recall side on the classify path too — internal/classify/classify.go:355-370's own comment concedes the `text`/`digits` split is owed (tracker T-0195) and that 'an SSN stored as bigint with no name hit is still none here'; T-0195 is the right home, but the comment should stop pointing at internal/verify's digits entry as the compensating control until (a) and (b) land, because today it is not one.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "NEW VARIANT of the A9b finding: the same SSNs in a `bigint`, but SPARSE (random over a 500M range) so verify's dense-sequence exemption cannot fire, with the same name-matched `msisdn numeric` phone neighbour present to supply corroboration.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2 && ./run.sh a9c a9c_numeric_sparse.sql --root public.payroll --take 300\ndocker exec rt3-src psql -U srcuser -d srcdb -At -c 'select taxref from payroll order by id limit 4'\ndocker exec rt3-tgt psql -U tgtuser -d tgtdb -c 'select id,taxref from payroll order by id limit 4'\n(schema: a9c_numeric_sparse.sql — taxref bigint := (100000000 + floor(random()*500000000))::bigint, 300 rows)",
   "leaked": true,
   "fix": "LEAKED. Exit 0, and the target's taxref values are byte-for-byte the source's (414491361, 355804648, 450966008, 170862209 on both sides). ValidNationalIDDigits returns true for all four (probed), the ratio is ~1.0 against a 0.97 threshold, and the range is not dense — so the ONLY thing suppressing the refusal here is guard (2) above, the corroboration gate reading a masked-by-name phone column as no neighbour at all. This is the sharper half of the finding: it isolates the corroboration bug from the dense-range bug, and it means fix (a) alone closes this variant while both (a) and (b) are needed for the contiguous-block form. Same fix; this variant is the regression test to write first, since it needs no sequence reasoning to explain.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 5 (A10) — a person's full name the dictionary does not carry, in a column called `label` whose name matches no rule, in a table with no other personal column. Khmer/Lao/Amharic transliterations (Sokhem Chanthavy, Bounmy Phimmasone, Tesfaye Gebreselassie, Rithy Sopheak, Khamla Vongsa).",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2 && ./run.sh a10 a10_dictmiss.sql --root public.entries --take 300\n./check.sh 'Sokhem' 'Bounmy' 'Tesfaye' 'Rithy' 'Khamla'",
   "leaked": true,
   "fix": "STILL LEAKED — 60 hits for each of the five names in the target dump, exit 0, `public.entries.label` reported as 'no name or value signal'. Commit 9f1b36b (T-0188, twenty more languages) did not reach these five; nothing in lazyslice.yml, the transcript or --json carries them, so again this is a target-rows-only leak.\n\nThis remains the acknowledged residual of THREAT_MODEL.md T1 lines 41 and 50, and I still do not think widening names.txt is the fix — the class is unbounded and the file's precision rules are what keep it usable. What HAS not been done, and is the actionable half round 2 named: the reason string. 'no name or value signal' is a finding of absence printed for a character column that was looked at and matched nothing; it reads as a clean bill of health and is the sentence an operator would cite in a review. Change it for character columns that were sampled to an absence-of-evidence phrasing — 'nothing recognised; not proof the column is impersonal' — and leave 'no name or value signal' for columns nothing could look inside (surrogate keys, FK columns, booleans). Cheap, honest, and it does not pretend the class is closed.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "NEW VARIANT of A10: the same dictionary-missing names in a `varchar(64)` column called `label`, but now in a table that also carries a `certain` personal column (`email text`), to test whether internal/classify's unknownColumnsBesideCertain rail covers the shape.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2 && ./run.sh a10b a10b_dictmiss_beside.sql --root public.entries --take 300\n./check.sh 'Sokhem' 'Bounmy' 'Tesfaye'",
   "leaked": false,
   "fix": "NOT LEAKED. 0 hits for all three names. `public.entries.label` is reported 'no name or value signal; raised by the neighbouring-column rule: public.entries has 1 columns at certain and nothing is known about this column's contents' and is masked. The rail works exactly as documented — which confirms that round 2's second suggestion (extend unknownColumnsBesideCertain to a table with NO certain column) is the whole of the remaining gap, and that the gap is precisely the one-personal-column table. No fix owed for this variant.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 6 — wrong target by cluster alias: reach one production cluster over two transports so sqlClusterID differs, and write to an empty production database on the source's own cluster named over a Unix socket.",
   "reproduction": "B=/private/tmp/claude-501/lazyslice-scratch/redteam-2/r5; mkdir -p $B/sock\n/opt/homebrew/bin/initdb -D $B/pgdata -U postgres --auth=trust\n/opt/homebrew/bin/pg_ctl -D $B/pgdata -o \"-p 15600 -h 127.0.0.1 -k $B/sock\" -l $B/pg.log start\n# databases app (customers/orders, 50 real rows) and newprod (same two tables, empty); role reader SELECT-only; REVOKE EXECUTE ON FUNCTION pg_control_system() FROM public\nexport LAZYSLICE_SECRET=$(openssl rand -hex 32)\n# CONTROL, TCP target on the same cluster:\n$LS --source 'postgres://reader:pw@127.0.0.1:15600/app' --target 'postgres://postgres@127.0.0.1:15600/newprod' --yes --no-config --root public.customers -n 5\n# ATTACK, same cluster, spelled as the socket:\n$LS --source 'postgres://reader:pw@127.0.0.1:15600/app' --target \"postgres://postgres@/newprod?host=$B/sock&port=15600\" --yes --no-config --root public.customers -n 5",
   "leaked": false,
   "fix": "NOT LEAKED (as reported). The two transport identities still differ at the SQL level (`2026-09-16 15:29:48.023685-04|127.0.0.1|15600` over TCP vs `...||` over the socket), but lazyslice no longer derives its cluster identity from them — commit b5e6f3d (T-0190). The attack run prints `! target newprod is on the same cluster as the source: this run writes to the source's own server`, exactly as the TCP control does (grep -c 'same cluster' = 1 on both). The transport-dependent false mismatch is closed.\n\nOne thing round 2's fix did not change and I flag as unchanged rather than as a new finding: the same-cluster rule is a WARNING, not a refusal, and under --yes the run proceeds and writes 5 rows into newprod on the production cluster in both the control and the attack. ARCHITECTURE.md §9 rule 1 is therefore advisory in a headless run. If that is intended it should say so; if not, --yes should make an unresolved same-cluster target a hard failure naming its own flag, the way the headless target rule (T-0184) already does for target selection.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 7 — catalog residue via table CHECK constraints carrying non-\"strong\" personal data: person name, postal address, and a special-category sentence, each in its own CHECK. Run isolated so each constraint is judged on its own.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2\n./run.sh c_name  c_name.sql  --root public.customers --take 10   # CHECK (full_name <> 'Aurelio Nakamura-Okonkwo')\n./run.sh c_notes c_notes.sql --root public.customers --take 10   # CHECK (notes IS NULL OR notes <> 'Priya Raghunathan disclosed her HIV diagnosis on 2019-04-02')\ndocker exec rt3-tgt psql -U tgtuser -d tgtdb -At -c \"SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE contype='c' AND conrelid<>0;\"",
   "leaked": true,
   "fix": "PARTLY FIXED, STILL LEAKED. The validator set behind strongCatalogHit / strongHit was genuinely widened (commit 986091d, T-0189): internal/plan/ddlliteral.go strongValidators and internal/verify/catalog.go strongCatalogHit now run ten validators including NameShape, addressLiteralShape and ProseName. The name CHECK is exit 13 ('parses as person_name'), the address CHECK is exit 13 ('parses as address'). But c_notes exits 0 and the target catalog holds:\n  ck_notes_free|CHECK (((notes IS NULL) OR (notes <> 'Priya Raghunathan disclosed her HIV diagnosis on 2019-04-02'::text)))\n\nRoot cause, verified. The widened list still has no entry for CatSpecial, and the free_text entry it does have is Dict.ProseName (internal/textsig/dict.go:219), which requires >= 6 whitespace fields AND an adjacent given-name/surname pair from names.txt. The literal has 8 fields, and `priya` IS in names.txt (line 9418) as a given name, but `raghunathan` is in neither list, so the adjacency never fires and ProseName returns false. Dict.NameShape on the whole 8-word string is false for the same reason. The literal therefore matches nothing and is replayed verbatim onto a column lazyslice itself masked as free_text.\n\nFix. (a) The DDL literal boundary must cover every category the row boundary masks: special_category has no validator at all in either strongHit or strongCatalogHit, which is the gap THREAT_MODEL.md T1's amendment claims is closed. Add the special-category term list the row path uses (the rules.yml special_category vocabulary — diagnosis/HIV/religion/union/orientation terms) as a literal validator in both functions. (b) ProseName is the wrong shape test for a DDL literal. Its dictionary-adjacency narrowing exists to stop a COLUMN OF prose being scored at exit 9 across thousands of rows; a single string literal written into a CHECK on a column the run classified personal is not a false-positive risk of the same size. For literals, fall back to plain Dict.Prose (>= 6 words with a dictionary hit) and, where that is still too noisy, fail towards the operator rather than towards silence — print the object and require an explicit --allow-ddl-literal opt-out, the way --allow-type-literal already works for enums and domains (commit a29b625). (c) The general rule, worth writing into THREAT_MODEL.md T1 in these words: the list of categories the DDL boundary recognises must be kept EQUAL to the list the row boundary masks, and a column the run decided to mask should make any literal in its own constraints refusable by default, not only literals that re-parse as a recognised shape.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "NEW VARIANT of the CHECK-constraint finding: special_category literals that are neither a person's name, nor an address, nor six words of prose — `CHECK (diagnosis <> 'HIV positive, CD4 210')` and `CHECK (religion <> 'Ahmadiyya Muslim')` — on two columns lazyslice itself masks as special_category by name alone.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2 && ./run.sh c_special c_special.sql --root public.customers --take 10\ndocker exec rt3-tgt psql -U tgtuser -d tgtdb -At -c \"SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE contype='c' AND conrelid<>0;\"",
   "leaked": true,
   "fix": "LEAKED, and this is the cleanest statement of the finding. Exit 0. The transcript prints, in the same run:\n  public.customers.diagnosis: name matches special_category; special category by name alone\n  public.customers.religion:  name matches special_category; special category by name alone\nand the target then holds:\n  ck_diag|CHECK (((diagnosis IS NULL) OR (diagnosis <> 'HIV positive, CD4 210'::text)))\n  ck_rel |CHECK (((religion IS NULL) OR (religion <> 'Ahmadiyya Muslim'::text)))\nlazyslice masks the column and ships the same category of data in the column's own constraint, under a green exit. Neither literal is long enough for any prose test or shaped like a name or an address, so no amount of tuning the existing ten validators reaches it — only fix (a) above, a special_category literal validator, or fix (c), refusing any non-pattern literal in a CHECK on a column the run classified personal unless an explicit opt-out names it. I would land (c) as the default and (a) as the thing that makes the refusal message say which category it saw: 'masked column, unrewritable literal in its own constraint' is a rule an operator can act on, and it does not depend on the literal happening to re-parse.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 8 — catalog residue via partial index predicates: CREATE INDEX ... WHERE <col> = '<name>' and WHERE <col> = '<address>', which internal/plan did not read at all (T-0163).",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2\n./run.sh c_idx      c_idx.sql      --root public.customers --take 10   # both predicates\n./run.sh c_idx_name c_idx_name.sql --root public.customers --take 10   # the person-name predicate alone\ndocker exec rt3-tgt psql -U tgtuser -d tgtdb -At -c \"SELECT pg_get_indexdef(indexrelid) FROM pg_index WHERE indpred IS NOT NULL;\"",
   "leaked": false,
   "fix": "NOT LEAKED. Both forms are exit 13 at plan: 'public.customers.idx_cust_addr ... a literal in the index this column parses as address' and 'public.customers.idx_cust_vip ... parses as person_name', code target.schema.literal_not_rewritable. Timing checked and it matters: the ✗ appears at line 15 of the transcript with NO 'dropping public.customers in the target' line anywhere before it, so the refusal is pre-drop and pre-load — both halves of round 2's fix landed (commit 986091d closes T-0163: internal/plan now reads pg_index). The target database is left with zero tables. No fix owed. Note the same category gap as the attempt above applies here too: an index predicate carrying the special-category literal would cross, which is one more reason to widen the literal validators rather than the read set.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 9 — catalog residue via enum labels, domain CHECK, domain DEFAULT and generated-column expressions carrying a non-strong category (person name, postal address twice, health disclosure sentence).",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2\n./run.sh cat3  schema3.sql  --root public.customers --take 10   # all four objects\n./run.sh cat3b schema3b.sql --root public.customers --take 10   # domain CHECK + DEFAULT alone\n./run.sh cat3c schema3c.sql --root public.customers --take 10   # generated expression alone\n./run.sh cat3d schema3d.sql --root public.customers --take 10   # generated expression with no address in the rendered value\ndocker exec rt3-tgt psql -U tgtuser -d tgtdb -At -c 'SELECT enumlabel FROM pg_enum;' -c \"SELECT typname, typdefault FROM pg_type WHERE typdefault IS NOT NULL AND typtype='d';\" -c \"SELECT attrelid::regclass||'.'||attname, pg_get_expr(adbin,adrelid) FROM pg_attrdef d JOIN pg_attribute a ON a.attrelid=d.adrelid AND a.attnum=d.adnum WHERE a.attgenerated<>'';\"",
   "leaked": false,
   "fix": "NOT LEAKED, all four objects. Enum label: exit 13, target.schema.type_literal, 'type public.case_owner carries a literal in label 1 that parses as person_name ... --allow-type-literal public.case_owner=REASON says it is not a person's'. Domain CHECK and DEFAULT: exit 13, 'type public.home_addr carries a literal in its definition that parses as address'. Generated column: exit 9 from the row second net ('public.customers.summary is not masked and 10 of its values validate as address'), and I re-ran it (cat3d) with the address removed from the rendered value so only the health sentence remained — still exit 9, and `SELECT ... FROM customers` in the target errors with 'relation \"customers\" does not exist', so the transaction rolled the target back and nothing persisted. Widening strongCatalogHit/strongHit (commit 986091d) closed the first three at once as round 2 predicted.\n\nCaveat worth recording rather than a finding: the generated-column arm is caught by the ROW net at exit 9, i.e. after load, not by the DDL pass at exit 13 before it. The target held the value for the length of the run and only the surrounding transaction saved it. A generated expression whose literal is a bare special_category term (see the c_special variant) would have neither control. The DDL pass, not the row net, should be the one that stops a generated expression.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 10 — bypass the literal net for a strong category by putting the value on the right-hand side of a pattern operator: CONSTRAINT ck_email_regex CHECK (email !~ '^ceo@bigcorp\\.example$'), where pipeline.Literal.Pattern exempted the literal from detection entirely.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2 && ./run.sh c_regex c_regex.sql --root public.customers --take 10\ndocker exec rt3-tgt psql -U tgtuser -d tgtdb -At -c \"SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='ck_email_regex';\"",
   "leaked": false,
   "fix": "NOT LEAKED. Exit 13: 'public.customers.ck_email_regex is masked and its recreated DDL carries a literal lazyslice cannot rewrite: a literal in the constraint this column parses as email'. The exemption is now detection-preserving: internal/verify/catalog.go strongCatalogHit and internal/plan/ddlliteral.go strongHit both begin `if lit.Pattern { s = strings.TrimSpace(pipeline.StripPatternMeta(s)) }` and then run the validators on the stripped text, exactly the shape round 2 asked for, so '^ceo@bigcorp\\.example$' reduces to the address and refuses while '%@%.%' reduces to '@' and does not. Commit 986091d. Target holds no such constraint. No fix owed.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 11 — a custom masker that returns its input verbatim, registered against the public mask module (THREAT_MODEL.md T12 / T-0180).",
   "reproduction": "Scratch module /private/tmp/claude-501/lazyslice-scratch/redteam-2/maskprobe (go.mod: module maskprobe; require github.com/Liarea/lazyslice/mask v0.0.0; replace github.com/Liarea/lazyslice/mask => <repo>/mask).\ntype passthrough struct{}; func (passthrough) Domain(c mask.Constraints) int64 { return 1<<40 }; func (passthrough) Mask(h [32]byte, in mask.Value, c mask.Constraints) (mask.Value, error) { return in, nil }\nmask.Register(\"rt3_passthrough\", mask.CatEmail, passthrough{})\nmask.Apply(k, mask.CatEmail, \"rt3_passthrough\", mask.Value{Text: \"victim.canary@bigcorp.example\"}, mask.Constraints{TypeTag: \"text\"})\ncd /private/tmp/claude-501/lazyslice-scratch/redteam-2/maskprobe && go run .",
   "leaked": false,
   "fix": "NOT LEAKED. Output: `PASSTHROUGH: out=\"\" masked=false err=the masker's output follows the value it was given, so the cell was not masked: category email: masker rt3_passthrough`. mask/mask.go maskCell now carries the post-condition (looksLikeItsInput + nothingToMask, with the documented carve-outs for a masked enum emitting a member label and a collapsed special_category), it names only the masker id and the category, and it returns nothing of the input. Commit f5ec705 (T-0191, implementing T-0180). No fix owed.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 12 — a custom masker that panics halfway with the cell value in its panic message (T7 / T-0181).",
   "reproduction": "Same maskprobe module: func (panicker) Mask(h [32]byte, in mask.Value, c mask.Constraints) (mask.Value, error) { panic(fmt.Sprintf(\"masker blew up halfway on %q\", in.Text)) }; mask.Register(\"rt3_panic\", mask.CatEmail, panicker{}); mask.Apply(...) wrapped in its own recover to see whether the panic escapes.\ncd /private/tmp/claude-501/lazyslice-scratch/redteam-2/maskprobe && go run .",
   "leaked": false,
   "fix": "NOT LEAKED. The panic no longer escapes the module: `PANIC-MASKER: out=\"\" err=the masker panicked: category email: masker rt3_panic panicked with a value of type string (it is withheld because it may quote the value)`. mask/mask.go maskCell's deferred recover converts it to ErrMaskerPanic naming only the masker, the category and panicKind(r) — and the comment there notes the guard also covers the second, sentinel generator call the post-condition makes, which was the subtlety worth getting right. Commit f5ec705 (T-0191, implementing T-0181). No fix owed.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 13 — a custom masker that RETURNS (rather than panics with) an error quoting the value it could not mask: the same free-form-string egress through the one door the panic amendment did not check.",
   "reproduction": "Same maskprobe module: func (erroring) Mask(h [32]byte, in mask.Value, c mask.Constraints) (mask.Value, error) { return mask.Value{}, fmt.Errorf(\"cannot mask %q: unsupported shape\", in.Text) }; mask.Register(\"rt3_error\", mask.CatEmail, erroring{}); mask.Apply(...)\ncd /private/tmp/claude-501/lazyslice-scratch/redteam-2/maskprobe && go run .",
   "leaked": true,
   "fix": "HALF FIXED; THE MODULE HALF STILL LEAKS. Output: `ERROR-RETURN: err=cannot mask \"victim.canary@bigcorp.example\": unsupported shape` — mask.Apply returns the masker's error verbatim.\n\nWhat landed: part (a) of round 2's fix is in. internal/transform/codes.go Refusal.Error() no longer interpolates r.Reason; it calls reasonSummary(r.Reason), which returns maskReason(err) for the module's own sentinel errors and otherwise 'an error of type %T (its message is withheld because it may quote the value)'. So the CLI half is closed and no lazyslice run prints this.\n\nWhat did not: mask/mask.go Apply -> maskCell still does `out, err = m.Mask(h, in, c); if err != nil { return Value{}, err }`, passing the third-party error straight out of the public module. mask/ is importable on its own (it has its own go.mod, and ADR-006 is the reason), so its contract cannot depend on the parent binary's redaction — the same argument that justified adding the recover two lines above applies unchanged to the error return beside it. Fix: in maskCell, wrap the masker's error the way the panic is wrapped — a new ErrMaskerRefused naming the masker id and the category and the error's %T, with the original kept only behind an explicitly opt-in unwrap (or not kept at all). Part (b) of round 2's fix is also still owed: cmd/lazyslice/main.go renderSafe is still an allowlist of two error types to scrub with `return err.Error()` as its default, so every new error type in the tree is value-bearing until someone notices; invert it as round 2 described. Built-in error messages are still clean today (grepped internal/ and mask/ for value-bearing formats), so this remains a contract fix, not a live CLI leak.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "NEW VARIANT of the error-return leak: probe every OTHER error mask.Apply can return, to see whether any of them interpolates the cell value — an unknown masker id, and Canonical failures on malformed jsonb, inet and numeric inputs carrying a canary.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2/maskprobe && go run .   # variant() in variant.go\n# cases: unknown-masker (CatEmail, id \"rt3_does_not_exist\"); canonical-badjson (CatSemiStruct, TypeTag jsonb, Text '{\"ssn\":\"078-05-1001\", BROKEN'); canonical-badinet (CatNetworkID, TypeTag inet, Text 'victim.canary@bigcorp.example'); canonical-badnumeric (CatFinancial, TypeTag numeric, Text '4539-1488-0343-6467-victimcanary')",
   "leaked": false,
   "fix": "NOT LEAKED. `unknown-masker` prints only the id it was given ('no masker is registered under that id: \"rt3_does_not_exist\"' — an id, not a value), and all three malformed-input cases fall through to the post-condition refusal, which names only the category and the masker. No production value reaches any of mask.Apply's own error returns; the masker's OWN returned error is the single remaining door, which narrows the fix above to exactly one wrap at one call site in maskCell rather than an audit of the module's error surface.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 14 — take the masking key out of the repository through a symlinked PARENT DIRECTORY, defeating a check that only os.Lstat'd the final path component.",
   "reproduction": "B=/private/tmp/claude-501/lazyslice-scratch/redteam-2; mkdir -p $B/sym/repo $B/sym/Dropbox\ncd $B/sym/repo && git init -q . && printf 'x\\n' > .gitignore && ln -s ../Dropbox cloudkeys\n$B/lazyslice --source 'postgres://srcuser:srcpw@127.0.0.1:55631/srcdb?sslmode=disable' --target 'postgres://tgtuser:tgtpw@127.0.0.1:55632/tgtdb?sslmode=disable' --root public.customers --take 10 --yes --secret-file ./cloudkeys/lazyslice.secret; echo exit=$?\nls -la $B/sym/Dropbox; cat $B/sym/repo/.gitignore",
   "leaked": false,
   "fix": "NOT LEAKED. Exit 5, secret.refused.symlink: './cloudkeys/lazyslice.secret is inside /private/.../sym/repo/cloudkeys, a symbolic link: the masking key would be written outside the repository, where .gitignore does not reach it'. The message names the symlinked DIRECTORY component, not the file, so the whole path is now resolved rather than only its last component. ../Dropbox is empty (no key written) and .gitignore still contains only 'x' — no false reassurance line. Commit aba1d68 (T-0192). No fix owed.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 15 — take the masking key out of the repository through a HARD link, which no path-based check can see.",
   "reproduction": "B=/private/tmp/claude-501/lazyslice-scratch/redteam-2; mkdir -p $B/hard/repo $B/hard/Dropbox\ncd $B/hard/repo && git init -q . && printf 'x\\n' > .gitignore\n$B/lazyslice --source ... --target ... --root public.customers --take 10 --yes    # creates ./lazyslice.secret 0600\nln lazyslice.secret ../Dropbox/leaked.key\n$B/lazyslice --source ... --target ... --root public.customers --take 10 --yes; echo exit=$?\nls -la ../Dropbox; diff lazyslice.secret ../Dropbox/leaked.key",
   "leaked": false,
   "fix": "NOT LEAKED. The second run is exit 5, secret.refused.hardlink: './lazyslice.secret has 2 names on disk: it may be readable under a path .gitignore never protected', printed as '✗ ./lazyslice.secret has more than one name on disk: it may already be readable under a path .gitignore never protected'. The Nlink > 1 stat round 2 asked for is in, and it reports alongside the existing mode check. Commit aba1d68 (T-0192).\n\nOne honest caveat, not a defect in the fix: the link is made between runs, so the key bytes are already under ../Dropbox/leaked.key before lazyslice ever sees the second name, and the refusal stops the next run rather than undoing the copy. That is the most any local check can do and the message says the right thing ('may already be readable'). Nothing owed beyond keeping that wording.",
   "attacker": "attack:3:r3"
  },
  {
   "attack": "REPLAY of round-2 attempt 16 — get a password into the committed lazyslice.yml through --password-command, which THREAT_MODEL.md T5 recorded as being stored 'as the command string'.",
   "reproduction": "cd /private/tmp/claude-501/lazyslice-scratch/redteam-2/runs/pw && /private/tmp/claude-501/lazyslice-scratch/redteam-2/lazyslice --source 'postgres://srcuser:srcpw@127.0.0.1:55631/srcdb?sslmode=disable' --target 'postgres://tgtuser:tgtpw@127.0.0.1:55632/tgtdb?sslmode=disable' --root public.benefit_claims --take 10 --yes --password-command 'vault read -field=pw secret/db # Hunter2InCommand'; echo exit=$?\ngrep -n -iE 'password|hunter' lazyslice.yml",
   "leaked": false,
   "fix": "NOT LEAKED. Exit 0 and lazyslice.yml contains no password_command key at all — grep for 'password' and 'hunter' returns nothing. The run prints: '! password_command was not written to ./lazyslice.yml: its argv looks like it embeds a value rather than fetching one; pass --password-command again on every run'. Both halves of round 2's fix landed: the argv heuristic plus the withheld-and-say-so warning (commit aba1d68, T-0192), and the no-op half separately (commit 22ae0b2, T-0213: '--password-command runs, and its output never reaches a sink'), so the flag the tool advertises now actually authenticates instead of sending operators back to PGPASSWORD. No fix owed.",
   "attacker": "attack:3:r3"
  }
 ]
}